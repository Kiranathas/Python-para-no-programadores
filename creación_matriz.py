#CREACIÓN DE UNA MATRIZ

matriz = []
cant_filas= int(input("ingrese cantidad de filas: "))
cant_columnas= int(input("ingrese cantidad de columnas: "))


while matriz [0] == matriz  [0]:
    if cant_filas != matriz:
        matriz.append(cant_filas)
        if  cant_columnas != matriz:
            matriz.append(cant_filas, cant_columnas)
print (matriz)